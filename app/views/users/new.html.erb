<% provide(:button_text, 'Create account') %>
<div id="clearance" class="sign-up">
  <h2><%= t(".title") %></h2>

  <%= form_for @user do |form| %>
    <%= render partial: '/users/form', object: form %>
    <br>
    <%= link_to t(".sign_in"), sign_in_path, class: "other-links" %>
  <% end %>
</div>
