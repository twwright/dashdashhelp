<!DOCTYPE html>
<html>

  <head>
    <title>Dashdashhelp</title>
    <%= render 'layouts/rails_head' %>
  </head>

  <body>
  <%= render 'layouts/header' %>
  
  <div class="container">
    <%= yield :errors %>
  </div>

   <div class="row">
     <div class="col-md-6 col-md-offset-3">
       <%= yield %>
     </div>
   </div>

  <%= render 'layouts/footer' %>
  <%# debug(params) if Rails.env.development? %>
  </body>
</html>
